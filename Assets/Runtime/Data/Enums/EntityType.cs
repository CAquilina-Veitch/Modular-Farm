namespace Runtime.Data.Enums
{
    // GDD 13.9 - Entity types (vehicles, mounts, etc.)
    public enum EntityType
    {
        Mount,      // Rideable animals (horse, etc.)
        Vehicle,    // Mechanical transport (car, etc.)
        Automator,  // Automated helpers (roomba, etc.)
        Pet         // Companion animals
    }
}
